- [x] `StoreState` 和 `Question` 类型接口已成功包含 `testMode` 和 `relationshipStage` 字段。
- [x] 首页（`Home.tsx`）存在明显的“赛博恋爱段位测试”（默认推荐）和“全景防PUA综合测试”两个独立入口。
- [x] 测试引擎能根据用户选择的模式，正确加载 20 道对应的恋爱题（分阶段），或 50 道混合场景的全景题。
- [x] `scoring.ts` 中的图鉴文案已被改写为极具攻击性、抽象和网感的风格（如包含“吸渣体质”等词汇）。
- [x] 所有页面在 1920px 的 PC 端分辨率下能居中展示，未出现过度拉伸，且完美适配移动端。