- [x] `useStore.ts` 和 `types/index.ts` 已成功增加针对不同 `category`（职场、家庭、情感）的独立积分状态 `categoryScores`。
- [x] `scoring.ts` 包含计算单场景抗压防御力 (0-100分) 的工具函数。
- [x] `scoring.ts` 包含生成“双标”（如职场/家庭分数差异极大）的吐槽短评工具函数。
- [x] 在图鉴海报（`Result.tsx`）中，不仅展示了总人格（如 PUAX），还清晰展示了三大场景的防御力得分图表或进度条。
- [x] 在图鉴海报（`Result.tsx`）中，展示了系统自动生成的极具网感的“双标鉴定”短评（例如“窝里横战神”等）。